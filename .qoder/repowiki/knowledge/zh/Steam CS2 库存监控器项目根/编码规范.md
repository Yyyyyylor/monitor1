- 所有配置项通过 `.env` 文件 + pydantic-settings 加载，`.env.example` 提供完整模板
- 异步应用统一通过 asyncio.run(main()) 启动，并在信号中断时按顺序关闭数据库连接与 HTTP 客户端
- 日志统一使用 loguru，通过 `setup_logging(settings.log_level)` 集中配置
- Docker 镜像以 `python -m src.main` 作为 ENTRYPOINT，数据与日志持久化到 `/app/data` 卷