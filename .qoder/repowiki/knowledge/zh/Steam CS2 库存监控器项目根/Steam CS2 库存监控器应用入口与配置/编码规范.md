- 模块内依赖通过函数内部的延迟 import 引入，避免循环导入和启动时加载所有子模块
- 配置项统一通过 pydantic-settings 的 BaseSettings 类定义，默认值集中声明在字段上，派生逻辑以 @property 形式暴露
- 异步入口函数统一使用 `async def` 并以 `asyncio.run()` 包裹调用，信号处理通过 `loop.add_signal_handler` 注册
- 日志统一通过 `setup_logging` 初始化，格式包含时间、级别、模块名、函数名、行号，输出到 stdout 和 data/logs 下的按日轮转文件