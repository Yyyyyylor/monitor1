- 测试函数统一使用 `from __future__ import annotations` 并标注 `-> None` 返回类型。
- 复杂被测对象通过局部辅助函数构造（如 `_make_item`、`_make_event`），避免在断言中重复拼装参数。
- 夹具按「原始数据 → 模型实例」分层定义（`sample_item_data` dict → `sample_item` Item），便于组合复用。
- 每个功能点一个独立测试函数，断言以 `assert` 直接检查返回值、长度、键存在性与字段值。
- 对 Steam API 相关输入统一通过 `mock_raw_inventory` fixture 提供，保持测试数据一致性。